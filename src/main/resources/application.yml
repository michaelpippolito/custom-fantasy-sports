spring:
  datasource:
    url: "jdbc:h2:file:M:\\Code\\local_files\\Fantasy_Database;DB_CLOSE_DELAY=-1"
    username: sa
    password: password
    driverClassName: org.h2.Driver
    initialization-mode: always
    schema: classpath:schema.sql
  h2:
    console:
      enabled: true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

mlb-year: 2023

throttle:
  amountMs: 2000